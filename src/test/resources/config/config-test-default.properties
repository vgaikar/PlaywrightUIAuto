#This is for test level specific.

# It is used for default fallback configuration, will check in config/config-test-default.properties
ENV=test-default
base.url=https://www.facebook.com
edit.username=validation05
edit.password=defaultPass
create.username=validation06
create.password=test

db.url=jdbc:h2:mem:testdb
db.username=sa
db.password=

logging.level=INFO
logging.config=log4j2-default.xml

report.path=reports/default/
report.format=html

timeout.seconds=30
retry.count=1

BROWSER=chrome
headless=true

enable.db=false
enable.reporting=true

# Below option will read screenshot mode as All or Failure from CI. 
screenshot.mode=FAILURE
#screenshot.mode=ALL